{
  "name": "bgld-roman-wb",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build:data": "node scripts/pipeline/build-data.mjs",
    "dev": "npm run build:data && astro dev",
    "build": "npm run build:data && astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "exceljs": "^4.4.0",
    "pagefind": "^1.5.2"
  }
}