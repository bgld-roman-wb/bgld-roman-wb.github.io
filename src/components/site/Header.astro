---
import SearchBox from './SearchBox.astro';
import LanguageSwitcher from './LanguageSwitcher.astro';
import Wordmark from './Wordmark.astro';
import type { Locale } from '../../i18n/ui';
import { localizePath, useTranslations } from '../../i18n/ui';

interface Props {
	lang: Locale;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<header>
	<div class="container header-inner">
		<div class="header-top">
			<p class="site-title">
				<a href={localizePath('/', lang)} aria-label={t('site.title')} style="color: inherit; text-decoration: none;">
					<Wordmark />
				</a>
			</p>
			<LanguageSwitcher lang={lang} />
		</div>
		<nav class="main-nav" aria-label="Main">
			<a href={localizePath('/browse/alphabetical/a', lang)}>{t('nav.browse')}</a>
			<a href={localizePath('/about', lang)}>{t('nav.about')}</a>
		</nav>
		<div class="search-row">
			<SearchBox lang={lang} />
		</div>
	</div>
</header>

<style>
	header {
		border-bottom: 1px solid var(--color-border);
	}
	.header-inner {
		display: flex;
		flex-wrap: wrap;
		gap: var(--space-2) var(--space-3);
		padding-block: var(--space-3);
	}
	.header-top {
		display: flex;
		flex-basis: 100%;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-3);
		min-width: 0;
	}
	.site-title {
		margin: 0;
		font-weight: 700;
		min-width: 0;
	}
	.site-title a {
		display: inline-flex;
		align-items: center;
		gap: var(--space-2);
	}
	.main-nav {
		display: flex;
		flex-wrap: wrap;
		gap: var(--space-2) var(--space-3);
		align-items: center;
		max-width: 100%;
		min-width: 0;
	}
	.search-row {
		flex-basis: 100%;
		min-width: 0;
	}
	@media (max-width: 40rem) {
		.main-nav {
			flex-basis: 100%;
		}
		.main-nav {
			margin-inline-start: 0;
		}
	}
</style>
